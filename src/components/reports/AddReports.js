import React from "react";
import PropTypes from "prop-types";

const AddReportsComp = (props) => {
  return (
    <div>
      <h4>Add Reports</h4>
    </div>
  );
};

AddReportsComp.propTypes = {};

export default AddReportsComp;
